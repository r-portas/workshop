{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["eslint", "typescript", "oxc", "jsdoc", "react", "react-perf", "unicorn"],
  "categories": {
    "correctness": "error",
    "suspicious": "error",
  },
  "rules": {
    // verbatimModuleSyntax is off (see tsconfig.json), so enforce `import type` here instead
    "typescript/consistent-type-imports": "error",
    // Prefer undefined over null
    "unicorn/no-null": "error",
    // Not needed for React 17+
    "react/react-in-jsx-scope": "off",
    // `_splat` is TanStack Router's fixed name for splat route params;
    // `_zod` is Zod's API for `toJSONSchema` unrepresentable handlers
    "eslint/no-underscore-dangle": ["error", { "allow": ["_splat", "_zod"] }],
  },
  "settings": {
    "jsdoc": {
      "tagNamePreference": {
        "remarks": "remarks",
      },
    },
  },
}
